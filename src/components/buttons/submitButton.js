import React from 'react';

const SubmitButton = () => {
    return (
        <button
            type="submit"
        >
            <i className="far fa-save"></i>
            <small>Save</small>
        </button>
    );
}

export default SubmitButton;